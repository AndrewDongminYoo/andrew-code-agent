# andrew-code-agent

Private macOS-first TypeScript CLI for personal use.

## Prerequisites

- Node.js 24.19.0
- pnpm 11.22.0
- Codex 0.148.0

## Development

```bash
pnpm install --frozen-lockfile
pnpm check
trunk check --all --no-fix
```

The package is private and must not be published.
